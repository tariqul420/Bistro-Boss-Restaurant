import { useEffect } from "react";

const OurMenu = () => {

    useEffect(() => {
        document.title = 'Our Menu || Bistro Boss Restaurant'
    }, []);

    return (
        <div>

        </div>
    );
};

export default OurMenu;